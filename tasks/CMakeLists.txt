# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(global_initializerTaskLib)
ADD_LIBRARY(${GLOBAL_INITIALIZER_TASKLIB_NAME} SHARED
    ${GLOBAL_INITIALIZER_TASKLIB_SOURCES})
add_dependencies(${GLOBAL_INITIALIZER_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${GLOBAL_INITIALIZER_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${QT_LIBRARIES}
    ${GLOBAL_INITIALIZER_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${GLOBAL_INITIALIZER_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${GLOBAL_INITIALIZER_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${GLOBAL_INITIALIZER_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${GLOBAL_INITIALIZER_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${GLOBAL_INITIALIZER_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${GLOBAL_INITIALIZER_TASKLIB_HEADERS}
    DESTINATION include/orocos/global_initializer)

